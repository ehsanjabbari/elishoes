# نسخه Debug - بررسی مشکل ویرایش فاکتور

## 🔍 نحوه تست و بررسی مشکل:

### 1. **آپلود فایل‌ها**
- فایل `elishoes-pwa-debug.zip` را extract کنید
- فایل‌ها را به GitHub repository آپلود کنید

### 2. **باز کردن Console**
در مرورگر (Chrome/Firefox):
- دکمه `F12` را بزنید یا `右键` → Inspect
- به تب `Console` بروید

### 3. **تست ویرایش فاکتور**
- روی دکمه "ویرایش" هر فاکتور کلیک کنید
- پیام‌های console را بررسی کنید

### 4. **پیام‌های مورد انتظار در Console:**

#### وقتی روی ویرایش کلیک می‌کنید:
```
Attaching edit listener to button with id: [invoice-id]
Edit button clicked for invoice: [invoice-id]
editInputInvoice called with id: [invoice-id]
showInvoiceModal called with: {action: "edit", id: "[invoice-id]", type: "input"}
Loading existing invoice for edit
Found invoice: {...}
Adding product row: {...}
addProductRow called with: {selectedProductId: "...", quantity: "5"}
Selected product: [product-name]
Set quantity: 5
Product row added successfully
Modal opened with data: {...}
```

### 5. **تشخیص مشکل:**

#### ✅ **اگر همه پیام‌ها ظاهر شوند:**
- مشکل در UI/UX است
- داده‌ها لود می‌شوند اما ممکن است نمایش داده نشوند

#### ❌ **اگر پیامی نمایش داده نشود:**
- مشکل در event listener است
- دکمه ویرایش به درستی کلیک نمی‌شود

#### 🔍 **اگر خطایی نمایش داده شود:**
- مشکل در function های JavaScript
- نیاز به اصلاح کد

### 6. **راه‌حل‌های متداول:**

#### مشکل: Event Listener
```javascript
// اگر دکمه کلیک نمی‌شود، مشکل در:
btn.addEventListener('click', handler);
```

#### مشکل: Modal نمایش داده نمی‌شود
```javascript
// بررسی کنید:
modal.classList.add('active');
```

#### مشکل: داده‌ها لود نمی‌شوند
```javascript
// بررسی کنید:
appState.sales151.find(s => s.id === id)
```

### 7. **گزارش مشاهدات:**

لطفاً نتایج console را برای من بفرستید:

1. **پیام‌هایی که نمایش داده می‌شوند**
2. **پیام‌هایی که نمایش داده نمی‌شوند**
3. **خطاهای JavaScript (اگر وجود دارند)**
4. **رفتار مورد انتظار vs رفتار واقعی**

### 8. **حذف Console Logs:**
بعد از پیدا کردن مشکل، console.log ها را حذف کرده و نسخه نهایی را ایجاد می‌کنیم.

---

**نکته**: این نسخه فقط برای debug است. بعد از پیدا کردن مشکل، نسخه clean ایجاد می‌شود.
